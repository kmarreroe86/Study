package headfirstpatterns.headfirst.command.remote;

public class RemoteControlTestMain {

	/*
	 * public static void main(String[] args) {
	 * 
	 * SimpleRemoteControl remote = new SimpleRemoteControl(); Light light = new
	 * Light(); Command lightOnCommand = new LightOnCommand(light);
	 * 
	 * remote.setSlot(lightOnCommand); remote.buttonWasPressed();
	 * 
	 * GarageDoor garageDoor = new GarageDoor(); GarageDoorOpenCommand garageCommand
	 * = new GarageDoorOpenCommand(garageDoor); remote.setSlot(garageCommand);
	 * remote.buttonWasPressed(); }
	 */
}
